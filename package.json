{
  "name": "StocksApp",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://raviteja83.github.io/StocksApp/",
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "gh-pages": "^0.12.0",
    "moment": "^2.18.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy" : "yarn run build && gh-pages -d build"
  }
}
